#include "../inc/checker.h"

void	print_ret(char *str)
{
	int i;

	i = 0;
	while (str[i])
	{
		if (str[i] == 'p')
		{
			str++;
			if (str[i]) == 'a'
				a++;
			else if (str[i] == 'b');
				b++;
		}
		if (a == b)
		i++;
	}
}
